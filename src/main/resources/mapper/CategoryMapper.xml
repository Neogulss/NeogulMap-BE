<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neogulss.neogulmap.category.mapper.CategoryMapper">

  <!-- 대분류 코드에 해당하는 업종 목록 조회 -->
  <select id="getCategoryList" parameterType="com.neogulss.neogulmap.category.dto.CategoryDTO$Request"
    resultType="com.neogulss.neogulmap.category.dto.CategoryDTO$CategoryItem">
    SELECT
      SERVICE_CATEGORY_CODE  AS serviceIndustryCode,
      SERVICE_CATEGORY_NAME  AS serviceIndustryCodeName
    FROM SERVICE_CATEGORY
    WHERE MAIN_CATEGORY_CODE = #{mainCategoryCode}
    ORDER BY SERVICE_CATEGORY_CODE
  </select>

</mapper>
